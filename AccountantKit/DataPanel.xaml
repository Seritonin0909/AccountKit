<UserControl x:Class="AccountantKit.DataPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccountantKit"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">
    <Border BorderBrush="Black" BorderThickness="1" Margin="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="textBlockDate" Grid.Column="0" Style="{StaticResource contentTextStyle}"/>
            <TextBlock x:Name="textBlockAmountOfMoney" Grid.Column="1" Style="{StaticResource contentTextStyle}"/>
            <Image x:Name="ButtonCancel" Source="Images/Cancel.png" PreviewMouseDown="ButtonCancel_PreviewMouseDown" Grid.Column="2" Width="80" Style="{StaticResource ImageButtonStyle}" />
        </Grid>
    </Border>
</UserControl>
